public abstract class Physical_Title extends Title
{
    private int copies;  // Number of copies distributed

    public Physical_Title(String title, String literatureType)
    {
        super(title, literatureType);
        this.copies = copies;
    }
}
